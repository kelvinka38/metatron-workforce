name: Workforce Autonomy P10 GS1 GS2 To GS3 GS4 — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'P10_LEGACY_AUTOFANOUT=DISABLED'
          echo 'Legacy automatic GS1/2 to GS3/4 handoff is archived; the explicit P10 controller owns sequencing.'

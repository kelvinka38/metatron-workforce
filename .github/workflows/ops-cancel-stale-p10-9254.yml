name: Cancel Stale P10 Controller 9254 — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'STALE_P10_9254_CLEANUP_ARCHIVED=PASS'
          echo 'One-time queue cleanup completed; automatic execution is disabled.'

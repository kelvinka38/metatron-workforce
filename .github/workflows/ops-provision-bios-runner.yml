name: Provision BIOS Production Runner — Deferred

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deferred:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'BIOS_DEDICATED_RUNNER_PROVISION=DEFERRED'
          echo 'Reason: no repository-runner administration credential is exposed to Actions.'
          echo 'Routine queue elimination does not depend on this optional capacity lane.'

name: BIOS Product v2 Production Closure Bridge — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived after BIOS Product #1 v2.0 P1-P9 production acceptance. See BIOS product/BIOS_PRODUCT_V2_ACCEPTANCE_EVIDENCE.md and ratification run 33467056010."

name: Inspect Point2 Indonesia Evidence — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'POINT2_DIAGNOSTIC_ARCHIVED=PASS'

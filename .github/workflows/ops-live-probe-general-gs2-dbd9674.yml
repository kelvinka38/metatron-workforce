name: Live Probe GS2 independent test 24a5d99

on:
  workflow_dispatch:
permissions:
  contents: read

jobs:
  inspect:
    runs-on: [self-hosted, linux, x64, metatron-prod]
    timeout-minutes: 5
    steps:
      - name: Read failed independent rerun evidence
        shell: bash
        run: |
          set -euo pipefail
          P=/tmp/workforce-p10-34026595022/gs2-independent-test-rerun.txt
          if [ -f "$P" ]; then
            echo 'INDEPENDENT_TEST_FILE_PRESENT=true'
            cat "$P"
          else
            echo 'INDEPENDENT_TEST_FILE_PRESENT=false'
          fi

name: One Shot P10 Closure — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'P10_LEGACY_AUTOFANOUT=DISABLED'
          echo 'Temporary exact-SHA one-shot closure is archived; use the explicit deep P10 controller.'

name: Break Legacy P10 Production Queue — Completed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'LEGACY_P10_QUEUE_BREAK_COMPLETED=PASS'
          echo 'Automatic P10 ratification is no longer part of routine deploy topology.'

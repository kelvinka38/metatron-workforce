name: Break Stale P10 Controller Queue — Completed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'STALE_P10_CONTROLLER_QUEUE_BREAK=PASS'
          echo 'Automatic workflow-only P10 queue generation is disabled.'

name: BIOS Product v2 Ratification — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Accepted by run 33467056010. Canonical evidence is recorded in BIOS product/BIOS_PRODUCT_V2_ACCEPTANCE_EVIDENCE.md."

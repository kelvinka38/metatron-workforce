name: phase3-workplace

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx384m -XX:MaxMetaspaceSize=192m -Dorg.gradle.workers.max=1 -Dorg.gradle.daemon=false"

concurrency:
  group: workforce-phase3-acceptance
  cancel-in-progress: true

jobs:
  phase3:
    runs-on: [self-hosted, linux, x64, metatron-prod]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '22'
          cache: gradle
      - name: Phase 3 acceptance
        run: ./gradlew clean test --tests 'com.metatron.workforce.phase3.Phase3WorkplaceAcceptanceTest' --no-daemon --max-workers=1
      - name: Full regression and build
        run: ./gradlew test bootJar --no-daemon --max-workers=1

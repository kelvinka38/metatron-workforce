name: Founder Authorized Deep P10 — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'FOUNDER_AUTHORIZED_P10_BOT_AUTODISPATCH=DISABLED'
          echo 'Deep P10 must be dispatched manually by the founder actor through the canonical controller.'

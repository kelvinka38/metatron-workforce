name: Retired - aquaculture-decision-synthesis-v2-build
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired legacy one-shot workflow; use canonical Highway ingress.'

name: Cancel Bot Deep P10 Runs — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'BOT_P10_CANCELLER_ARCHIVED=PASS'

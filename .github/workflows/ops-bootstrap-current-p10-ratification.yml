name: Bootstrap Current P10 Ratification — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'P10_BOOTSTRAP_AUTORUN=DISABLED'
          echo 'Deep ratification is explicitly invoked only for a deployed SHA after the fast Highway.'

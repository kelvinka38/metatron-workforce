package com.metatron.workforce.interaction.llm;

/** Supported intelligence-provider identities. Transport and credentials are implementation concerns. */
public enum LlmProvider {
    OPENAI,
    ANTHROPIC,
    GOOGLE
}

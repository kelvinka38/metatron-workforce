/**
 * Metatron Intelligence package.
 *
 * <p>Production composition MUST enter provider cognition through
 * {@link com.metatron.workforce.interaction.intelligence.InstitutionalIntelligenceRuntime}
 * and the shared {@link com.metatron.workforce.interaction.intelligence.IntelligenceFabric}.
 * Provider/model sessions are replaceable cognition capacity only; they are never Worker identity,
 * authority, authorization, institutional memory, evidence, execution or outcome.</p>
 *
 * <p>Cognitive Runtime compatibility note: constructors or overloads that directly accept raw
 * provider credentials or omit canonical request correlation exist only for tests/migration and
 * MUST NOT be used to create a second production-owned provider router/policy loop. The historical
 * architecture documents 01/02 are compatibility references only where they conflict with the
 * Founder-approved 04/05/06 Cognitive Runtime baseline.</p>
 *
 * <p>The production default is zero frontier calls when deterministic/retrieval state is sufficient,
 * otherwise one frontier call. Any later call must remain bounded and reason-coded.</p>
 */
package com.metatron.workforce.interaction.intelligence;

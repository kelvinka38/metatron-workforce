package com.metatron.workforce.interaction.intelligence;

/** Exact arithmetic operations that do not require frontier reasoning once operands are known. */
public enum DeterministicComputationOperation {
    SUM,
    AVERAGE,
    DIFFERENCE,
    PRODUCT,
    DIVIDE,
    PERCENT_OF,
    PERCENT_CHANGE
}
